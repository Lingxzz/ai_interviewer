# PDF解析库（至少安装一个）
pypdf>=3.0.0
pdfminer.six>=20221105
PyMuPDF>=1.23.0

# 使用旧版 chromadb（不依赖 opentelemetry）
chromadb==0.4.22

# 其他依赖
streamlit>=1.28.0
langchain>=0.1.0
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# 固定 protobuf 版本防止冲突
protobuf==3.20.3
